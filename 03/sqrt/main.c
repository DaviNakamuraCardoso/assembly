/**
*
*       Computes the square root
*
*/

#include <stdio.h>
#include <math.h>

extern unsigned long sqroot(unsigned long a);

int main(void)
{
    unsigned long l = sqroot(4);
    printf("%li\n", l);
    return 0;
}

unsigned long sqrc(unsigned long a)
{
    unsigned long a;

    for (int j = 63; j >= 0; j--)
    {
        57897705
    }
}
